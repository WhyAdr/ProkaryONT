PROKARYONT PIPELINE CHANGELOG
=============================

Date: 2026-08-25
Author: Antigravity (AI coding assistant)

Comprehensive Conda Environment Setup and Runtime Compatibility Hardening
-------------------------------------------------------------------------
1. Created `conda-environment-setup-for-each-prokaryont-script.md` providing audited, isolated per-stage Conda/Mamba environment specifications (`prokaryont-stage1-qc` through `prokaryont-stage5-taxonomy`) and Slurm HPC batch templates.
2. Documented mandatory `-c nanoporetech` channel requirement for `fastcat` to prevent package resolution failures under strict channel priority.
3. Standardized SNIKT installation to use the official Bioconda `snikt` recipe (resolving `seqtk` and R dependencies automatically) and documented the `snikt.R -> snikt` binary alias check.
4. Documented `nextdenovo -> nextDenovo` case-sensitivity symlink compatibility for Stage 3 pre-flight validation on Linux filesystems.
5. Added explicit `openjdk>=11` to the Stage 3 environment specification to guarantee Java runtime and JVM heap stability for Canu overlap/gatekeeper jobs.
6. Updated `README.md` with links and operational guidance for the modular environment setup.

Date: 2026-08-20
Author: Codex

patch-2008: Modular Stage Execution and Autocycler Extensions
-------------------------------------------------------------
1. Archived the former `prokaryont.sh`/`run_all.sh` master orchestration and Stage 6-7 annotation stack; active Stages 1-5 are now invoked explicitly and independently.
2. Corrected direct-stage CLI > config > default precedence across active Stages 1-5.
3. Made Filtlong `--keep_percent` opt-in and added wrapper-level range validation.
4. Added direct Stage 3 `--assemblers` selection, LJA/iLesta helpers, and backward-compatible `wtdbg2` to `redbean` normalization with duplicate suppression.
5. Added native Autocycler or independently seeded Rasusa subsampling while preserving the `sample_NN.fastq` downstream contract and recording Rasusa metadata.
6. Added explicit, independently capped `--compress-threads` and `--combine-threads` controls (1-100).
7. Wired the full Stage 3 read set, configurable depth k-mer, and capped threads into `autocycler combine` depth annotation.
8. Added a non-mutating, default-on Plassembler PLSDB screen of every consensus contig with validated top-level `plassembler_summary.tsv` output.
9. Added optional Dorado polish device selection, applied to both polish passes but not to Dorado basecalling or alignment.
10. Expanded mock tools, cleanup, configuration, and documentation for the new modular workflow.

Date: 2026-08-19
Author: Codex

Stage 1 SDUST Assessment and Fastcat Lint Reliability
------------------------------------------------------
1. Added a non-destructive Fastcat SDUST census to `01_qc_estimate.sh`, producing raw stderr provenance, a positive-read TSV, a distribution histogram, a key/value summary, and a high-burden candidate list under `01_qc/fastcat_sdust/`.
2. Added Stage 1 `--lint-threshold` and `--lint-window` overrides with CLI > config > default precedence; these shared SDUST parameters now govern both the Stage 1 assessment and optional Stage 2 filtering.
3. Documented SDUST assessment outputs and the Stage 1-to-Stage 2 review workflow in `pipeline.conf`, `README.md`, and `testing_guidelines.md`.
4. Changed Stage 2 Fastcat lint logging to overwrite its rejection log per run and count rejections safely when none are present.
5. Enhanced the mock Fastcat binary with representative zero-SDUST and SDUST-positive output for structural assessment testing.

Date: 2026-07-12
Author: Antigravity (AI coding assistant)

Dorado Trim, Double-Polish Pass, & Dnaapler Reproducibility
------------------------------------------------------------
1. Added explicit --trim all to dorado basecaller in 04_polish_orient.sh (defensive; already default but future-proofs against changes).
2. Added opt-in --double-polish flag to 04_polish_orient.sh: runs a second Dorado polish pass after dnaapler reorientation to correct residual errors at the old linearisation breakpoint where reads previously soft-clipped.
3. Coordinated --cleanup-bam with --double-polish: all_reads_w_moves.bam is retained through Step 11 when double-polish is active, then cleaned up after the second pass.
4. Added -a nearest (fallback autocomplete for divergent genomes) and --seed_value 13 (reproducible BLAST seed) to the dnaapler all command.
5. Added double_polish and cleanup_bam config entries to pipeline.conf with explanatory comments.
6. Updated run_all.sh to forward --double-polish and --cleanup-bam from config to 04_polish_orient.sh.

Date: 2026-07-07
Author: Antigravity (AI coding assistant)

Summary of Diagnostics, Tunability, and Numbering Updates
----------------------------------------------------------
1. Added read retention report to 02_preprocess_filter.sh, warning users if less than 65% of reads survive filtering.
2. Implemented per-step read counts logging after Porechop, Chopper, and Fastcat lint steps in 02_preprocess_filter.sh.
3. Exposed the min_depth_rel relative depth threshold as a tunable parameter in 03_autocycler_assemble.sh (CLI option --min-depth-rel) and pipeline.conf (default: 0.1).
4. Configured 03_autocycler_assemble.sh to dynamically map <assembler>_extra_args configuration settings from pipeline.conf to individual assembler executions.
5. Defined a generic, shell-portable _float_gt helper function in 03_autocycler_assemble.sh to standardize floating-point calculations and comparisons.
6. Added check for SIGKILL (signal 9) in GNU parallel job logs to detect and report out-of-memory (OOM) failures for assembler jobs.
7. Fixed non-contiguous step numbering gaps in 03_autocycler_assemble.sh by mapping CURATION POINTs to steps 9g, 9i, 9k, resulting in a clean, contiguous 9a–9o step sequence.
8. Replaced GFA file size approximation check inside generate_dotplots with precise sequence length computed via GFA S-line bp sum.
9. Added descriptive comment clarifying the PROKARYONT_RESUME=dnaapler logic branch in 03_autocycler_assemble.sh.

Date: 2026-06-27
Author: Antigravity (AI coding assistant)

Summary of Bug-Fixes and Robustness Updates
-------------------------------------------
1. Fixed fastplong execution in 02_preprocess_filter.sh to write to its default reports instead of /dev/null, explicitly disabling modifying operations using -Q -L -A.
2. Added cross-script step references to 02_preprocess_filter.sh and documented EXIT trap behavior.
3. Fixed dry-run temp file leaks of Autocycler weighting in 03_autocycler_assemble.sh.
4. Added existence/non-empty guards for trimmed GFAs before calling autocycler dotplot or MUMmer self-map alignments.
5. Rewrote Autocycler resolve log metrics extraction (anchors, unique/conflicting bridges, unitigs, total length) with robust, keyword-anchored awk parsers.
6. Apportioned thread counts 3:1 between minimap2 mapping and samtools sort to prevent node oversubscription (2x thread concurrency peak).
7. Deferred contig characteristics join (deleted contig_characteristics.tsv unified output) and simplified step 9q to log a clean standalone contig depth summary utilizing the new awk exit check float comparison (replacing bc -l dependency).

Date: 2026-06-27
Author: Antigravity (AI coding assistant)

Summary of Chopper Tweaks
-------------------------
1. Upgraded chopper preprocessing to support all 4 Rust-based trimming strategies:
   `fixed-crop`, `trim-by-quality`, `best-read-segment`, and `split-by-low-quality`.
2. Changed the CLI key from `--quality` to `--cutoff` in `02_preprocess_filter.sh`
   after direct verification against the official Chopper repository.
3. Added the `--split-window` parameter to `02_preprocess_filter.sh` and routed
   it through `run_all.sh` and `pipeline.conf`.
4. Formally renamed Step 6 header and log outputs to "Step 6a" to align with "Step 6b" (Fastcat lint).

Date: 2026-06-27
Author: Antigravity (AI coding assistant)

Summary of Enhancements
-----------------------
1. Integrated standalone reads path flexibility into 03_autocycler_assemble.sh
   supporting both `--reads` and `--input-fastq` aliases.
2. Improved missing-file error hints in 03_autocycler_assemble.sh validation.
3. Integrated the NECAT assembler into Stage 3 (assembler_list, require_tool check).
4. Implemented optional Step 6b (Fastcat lint SDUST low-complexity filtering)
   in 02_preprocess_filter.sh with native gzip input handling, intermediate
   uncompressed streaming, and dedicated reject log (01_qc/02_lint_rejected.log).
5. Forwarded Fastcat lint options through run_all.sh and added documentation
   keys to pipeline.conf.

Date: 2026-06-25
Author: Antigravity (AI coding assistant)

Summary of Fixes (Post-Code Review)
-----------------------------------
1. Fixed input_fastq bug in 03_autocycler_assemble.sh: replaced stale
   ${input_fastq} reference with ${filtered_reads} at metrics collection.
2. Replaced grep -oP Perl regexes in 03_autocycler_assemble.sh with POSIX-
   compliant grep -oE equivalents to eliminate locale warnings on Windows/Git Bash.
3. Enhanced temp file cleanup in 02_preprocess_filter.sh: implemented a global
   trap handler on EXIT to clean up all intermediate files (_trim_tmp,
   _crop_tmp, and _qfilt_tmp) under normal execution or unexpected failure.
4. Corrected style/indentation of Dorado polishing else-block in 04_polish_orient.sh.

Date: 2026-06-25
Author: Antigravity (AI coding assistant)

This changelog records the Stage 2/3 pipeline split, full STEP renumbering
(STEP 1-17 across the core pipeline), and the preprocessing diagnostics
overhaul: Fastcat/Porechop_ABI/SNIKT diagnostics in Stage 1; optional
Porechop_ABI trim, SNIKT re-assessment, and optional Chopper end-trim in
Stage 2; Fastplong demoted to QC-reporting only (its Q-score formula uses a
flat arithmetic Phred mean with integer truncation, confirmed via source
read to diverge from Seqkit/Chopper's probability-space mean — not used
for filtering). RagTag removed pending a dedicated post-assembly script.

Summary of Changes
------------------

1. 02_filter_assemble.sh → 02_preprocess_filter.sh + 03_autocycler_assemble.sh (Pipeline Split)
   - Split the monolithic filter+assemble script into two independently
     runnable stages, handed off via filtered_input.fastq.gz.
   - Consolidated the previously-duplicated genome-size recovery logic into
     one block (now STEP 9a).
   - Removed RagTag scaffolding entirely (was tail of the old script) —
     deferred to a future dedicated script; --ragtag now logs an explicit
     no-op warning instead of silently doing nothing.
   - Renumbered every in-file STEP label across the core pipeline (STEP
     1-17, see major_overhaul_implementation_plan_REFINED_v2.md §4 for the
     full map).

2. 01_qc_estimate.sh (Preprocessing Diagnostics)
   - Added Fastcat (per-file stats + histograms), Porechop_ABI ab-initio
     discovery scan, and SNIKT baseline contamination report as STEP 2,
     all hard dependencies (require_tool) of this script going forward.

3. 02_preprocess_filter.sh (Trimming additions)
   - Added optional Porechop_ABI adapter trimming (STEP 4) and optional
     Chopper end-trimming via --trim-approach (STEP 6), both off by
     default and meant to be enabled only after reviewing Stage 1/Stage 2
     SNIKT and Porechop_ABI diagnostics.
   - Added SNIKT re-assessment (STEP 5) and Fastplong QC reporting (STEP 8,
     report-only — see rationale above).

4. run_all.sh / prokaryont.sh (Renumbering)
   - run_all.sh now orchestrates Stages 1-4. prokaryont.sh usage text and
     taxonomy/annotate dispatch updated for the 05_/06_ renumbering.

--------------------------------------------------------------------------------

Date: 2026-06-21
Author: WhyAdr & Antigravity AI Pair Programmer

This changelog records the bug fixes and refinements implemented across the pipeline scripts to address logic errors, performance constraints, and robustness issues.

Summary of Changes
------------------

1. 00_setup.sh (Foundation Utilities)
   - Changed `log_error` exit behavior from soft return to a hard `exit 1` to prevent continuation under `||` chains or function scopes.
   - Replaced static `GZIP_BIN` export with dynamic `get_gzip_cmd()` function to properly support custom thread allocations at execution time.
   - Restricted `load_config` keys strictly to lowercase snake_case (`^[a-z][a-z0-9_]*$`) to protect system environment variables from configuration overrides.
   - Enhanced dry-run command logging format using `printf %q` to preserve shell-escaped command structure.
   - Hardened log file initialization by checking write permission and falling back to `/dev/null` on failure.

2. 01_qc_estimate.sh (QC & Genome Estimation)
   - Integrated dynamic GZIP command mapping.
   - Fixed `free -g` parsing of available memory by adding a fallback column check.
   - Reordered `meryl count` arguments to correctly support version 1.4+.
   - Added heuristic comment for the genome size calculation weighting.
   - Guarded `raven_size` calculation in dry-run mode to prevent parsing stale assembly files.
   - Fixed carriage return accounting (`tr -d '\r\n'`) to prevent CRLF line endings from inflating computed sizes.
   - Cleaned up intermediate Raven GFA files after size computation to conserve disk space.
   - Supported a configurable `lrge_seed` parameter (default 123) for reproducibility.
   - Documented the step gap (Step 2 resides in `02_filter_assemble.sh`).

3. 02_filter_assemble.sh (Assembly & Filtering)
   - Front-loaded `read_type` validation.
   - Restored detailed multi-line comment rationale on Seqkit quality filtering.
   - Added `pipefail` to Filtlong and Minimap2 commands to catch silent pipeline crashes.
   - Overhauled GNU Parallel exit code checks to correctly distinguish and report OOM (signal) kills and count job errors from joblogs.
   - Replaced temporary file creation for ETE3 checking with direct Python heredoc execution.
   - Updated ETE3 leaf parser to correctly strip quotes and parse arbitrary leaf name suffixes.
   - Added `nullglob` checks for combine GFAs to handle empty cluster cases gracefully.
   - Added pipeline fail fallback to Seqkit stats parsing.
   - Replaced slow `samtools depth` parsing with efficient `samtools coverage` summaries.
   - Aligned job-building loops indentation to match else scope.
   - Fixed carriage return accounting (`tr -d '\r\n'`) in curation diagnostics.
   - Passed `--nice 19` option explicitly to parallel itself rather than using the `nice` utility.

4. 03_polish_orient.sh (Polishing & Reorientation)
   - Added dynamic GZIP setup and pipefail logic.
   - Added dry-run guard to renaming operations.
   - Removed CPU-centric `--infer-threads` flag from Dorado polish for GPU polishing runs.
   - Implemented re-run idempotency checks to skip Dnaapler when final outputs are present.
   - Replaced fragile tab escaping regex in SAM RG unification with C-string quoting (`$'...'`).
   - Standardized use of `samtools quickcheck -u` across all alignments validation checks.
   - Copied final reoriented assemblies to output instead of symlinking.

5. run_all.sh (Master Orchestrator)
   - Forwarded `skip_curation` systematically to all child scripts via `common_flags`.

--------------------------------------------------------------------------------

Date: 2026-05-22
Author: Antigravity AI Pair Programmer

This changelog records the recent enhancements made to the Prokaryote Genome Assembly pipeline (ProkaryONT) scripts to capture extensive runtime diagnostics, optimize input statistics handling, and surface critical polishing and reorientation details.

Summary of Changes
------------------

1. 02_filter_assemble.sh (Assembly & Diagnostics Upgrades)
   - Standardized Consensus Path: Set default autocycler_consensus output path to point directly to `autocycler_out/consensus_assembly.fasta` in all stages rather than moving it to the root path.
   - Autocycler Cluster Diagnostics: Captured standard out/error logs of `autocycler cluster` to parse and output clean cluster distances, QC pass/fail status, and reasons using `awk` to the pipeline log.
   - Comprehensive Per-Cluster Metadata Logging: Introduced `autocycler_out/cluster_metadata.tsv` which compiles extensive per-cluster resolution metrics during the trim & resolve loop:
     * Overlaps trimmed (circular start-end and hairpin)
     * MAD filtering parameters (median, MAD value, allowed range)
     * Contig retention status (kept vs. excluded count)
     * Bridge resolution details (anchors, unique bridges, conflicting bridges)
     * Final unitig counts and total resolved assembly length
   - Combined Assembly Logging: Captured `autocycler combine` console logs to print combined assembly topology (circularity and length statistics) automatically.
   - Smart Metrics Prepending: Refactored the update step of `metrics.tsv` to detect and safely fill in existing input read metric columns (e.g. read count, bases, N50) instead of blindly prepending them, ensuring compatibility with newer versions of Autocycler.
   - Combined Contig Characteristics Report: Added Step 4q which merges per-cluster assembly metrics from `cluster_metadata.tsv` with depth coverage from `contig_depths.tsv` into a unified `contig_characteristics.tsv` report.
   - Enhanced Contig Summary Logging: Outputs a high-fidelity tabular summary in pipeline log for all contigs, flagging high-copy-number replicons with relative depth > 1.5×.

2. 03_polish_orient.sh (Reorientation Diagnostics)
   - Dnaapler Logging Integration: Added parsing logic to automatically search the `dnaapler_out/*.log` directory.
   - Diagnostic Surfacing: Extracts and prints reorientation hits, rotation coordinates, and start genes (such as dnaA, repA, terL) directly to console/pipeline run logs, making rotation decisions transparent.

3. run_all.sh (Master Orchestrator Standardization)
   - Unified Consensus Filepath: Updated Stage 3's arguments to invoke `autocycler_out/consensus_assembly.fasta` instead of the root-level path.
   - Key Outputs summary: Updated the final stage completion report to list `autocycler_out/consensus_assembly.fasta` as the primary consensus artifact.
